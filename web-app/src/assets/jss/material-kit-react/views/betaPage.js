import styles from "./commonPages.js";

const betaPageStyle = {
   ...styles,
   container: {
      borderRadius: "10px",
      padding: "10px 25px",
      backgroundColor: "rgba(0,0,0,0.9)",
      textAlign: "center"
   }
};

export default betaPageStyle;
