const renderCardsStyle = {
   cards: {
      display: "flex",
      width: "100%",
      height: "100%",
      justifyContent: "center",
      flexFlow: "row wrap"
   }
};

export default renderCardsStyle;
