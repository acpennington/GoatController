const resizableContainerStyle = {
   container: {
      display: "flex",
      justifyContent: "center",
      alignItems: "center",
      margin: "auto",
      width: "100vw",
      height: "100vh"
   },
   innerContainer: {
      display: "flex",
      flexDirection: "row",
      overflow: "hidden"
   }
};

export default resizableContainerStyle;
